apply plugin: "war"

bootWar {
    mainClassName = "com.tremend.poli.meets.mentorship.MentorshipServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
